export const body: HTMLElement = document.body;
export const html: HTMLElement = document.documentElement;
export const main: HTMLElement = document.getElementById('main');
export const CSS: CSSStyleDeclaration = window.getComputedStyle(html);
