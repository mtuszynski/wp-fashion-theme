<a href="{{site.url}}" title="home">
  {% if site.logo.src != null %}
    <img class="" src="{{site.logo.src}}" alt="{{site.name}}" width="154" height="50">
  {% else %}
    <span class="">{{site.name}}</span>
  {% endif %}
</a>
